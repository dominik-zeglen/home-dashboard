[Unit]
Description=Node health status service
After=network.target

[Service]
ExecStart=/usr/bin/python /opt/dashboard/server.py
WorkingDirectory=/opt/dashboard
Restart=always
User=status
Environment=PORT=18745

[Install]
WantedBy=multi-user.target
